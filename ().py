# coding: utf-8
from todo_list.models import User, Categories, Todo, Events
from django.utils import timezone
t = Todo(name = "Thing 1", created_at=timezone.now())
Todo.objects.all()
get_ipython().run_line_magic('save', '()')
Todo.objects.all()
from todo_list.models import User, Categories, Todo, Events
Todo.objects.all()
t = Todo(name = "Thing 1", created_at=timezone.now())
get_ipython().run_line_magic('save', '()')
